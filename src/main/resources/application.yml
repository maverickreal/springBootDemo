spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5332/customers
    username: user
    password: password
  jpa:
    generate-ddl: true
    open-in-view: false
    show-sql: true
    hibernate:
      ddl: auto-update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  main:
    web-application-type: servlet